import React from "react";
import { ScrollView, View } from "react-native";

export default function Login({}) {
  return <ScrollView style={{ flex: 1, backgroundColor: "red" }}></ScrollView>;
}
